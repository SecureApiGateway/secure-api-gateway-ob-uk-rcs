# Configuration to enable no intentTypes, individual tests can override this configuration via test level property source
consent:
  store:
    enabled:
      intentTypes:

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.4.0